[project]
name = "predictive-maintenance"
version = "0.1.0"
description = "Vanilla data science project for predictive maintenance using the UCI AI4I 2020 dataset."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "jupyter",
    "matplotlib",
    "notebook>=7.6.2",
    "numpy",
    "pandas",
    "scikit-learn",
    "seaborn",
]
